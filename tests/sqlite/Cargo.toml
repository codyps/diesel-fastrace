[package]
name = "fastrace-diesel-sqlite-tests"
version = "0.0.0"
edition = "2024"
publish = false

[dependencies]
fastrace-diesel = { path = "../.." }
diesel = { version = "2.3.0", default-features = false, features = ["sqlite"] }
fastrace = { version = "0.7.19", features = ["enable"] }
url = "2.5.4"

[[test]]
name = "sqlite"
path = "sqlite.rs"
